# Filesystems (user guide)

Synthetic corpus enumeration is available for NTFS, FAT32, exFAT, ext4, and APFS path models. Real raw volume parsers remain progressive; see capability matrix for honest Limited notes.

Deleted recovery (R1): names retained with `partial` / `ambiguous_deleted_name_retained` labels only — not a claim of full undelete.
