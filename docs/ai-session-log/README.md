# AI session logs

One markdown file per autonomous continuation session.

Naming: `YYYY-MM-DD-<short-slug>.md`

Each log should record: prior tip SHA / checklist state, goal, done, verification commands, handoff next step.
