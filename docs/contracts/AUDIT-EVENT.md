# Audit Event Contract

**Schema version:** audit-event-1

Audit records security- and workflow-relevant user/system actions (open case, role assumption, scope override, export, authorization, recovery).

Uses the same envelope fields as provenance events. Payload includes action code, target references, authorization references, and outcome.
