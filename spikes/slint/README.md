# Spike: Slint + Rust

Gate A candidate `C-SLINT`.

## Intent

Prove whether Slint with an authoritative Rust core can satisfy Trareon Lab mandatory gates for virtualized large tables, packaging/signing, accessibility, and offline installers without a system webview.

## Required equal workflow

See `../README.md`. Implement the synthetic million-row case workflow with Rust owning all case logic.

## Status

Scaffold only. No measurement recorded.
