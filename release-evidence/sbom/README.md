# SBOM outputs (CI)

Generated by the `sbom` job in `.github/workflows/ci.yml`.

Files written at build time (not necessarily committed):

- `sbom-cargo-metadata.json` — full `cargo metadata` JSON
- `package-inventory.txt` — sorted `name@version` inventory

Consumers: release notes for unsigned builds; vulnerability review alongside `cargo audit`.
