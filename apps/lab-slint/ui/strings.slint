// Chrome copy for EN / ID. Display strings live here only.

export global Strings {
    in-out property <string> locale: "en";
    out property <bool> is-id: locale == "id";

    out property <string> brand: "Trareon Lab";
    out property <string> nav-group-case: is-id ? "KASUS" : "CASE";
    out property <string> nav-group-examine: is-id ? "PERIKSA" : "EXAMINE";
    out property <string> nav-group-work: is-id ? "HASIL KERJA" : "WORK PRODUCT";
    out property <string> nav-group-workflow: is-id ? "ALUR KERJA" : "WORKFLOW";
    out property <string> nav-group-lab: "LAB";
    out property <string> nav-group-support: is-id ? "DUKUNGAN" : "SUPPORT";
    out property <string> chip-case: is-id ? "KASUS" : "CASE";
    out property <string> chip-coverage: is-id ? "CAKUPAN" : "COVERAGE";
    out property <string> chip-integrity: is-id ? "INTEGRITAS" : "INTEGRITY";
    out property <string> nav-case: is-id ? "Kasus" : "Case";
    out property <string> nav-evidence: is-id ? "Bukti" : "Evidence";
    out property <string> nav-hex: "Hex";
    out property <string> nav-artifacts: is-id ? "Artefak" : "Artifacts";
    out property <string> nav-search: is-id ? "Cari" : "Search";
    out property <string> nav-timeline: is-id ? "Linimasa" : "Timeline";
    out property <string> nav-bookmarks: is-id ? "Markah" : "Bookmarks";
    out property <string> nav-graph: "Graph";
    out property <string> nav-runs: "Runs";
    out property <string> nav-report: is-id ? "Laporan" : "Report";
    out property <string> nav-transfer: "Transfer";
    out property <string> nav-capabilities: is-id ? "Kapabilitas" : "Capabilities";
    out property <string> nav-about: is-id ? "Tentang" : "About";
    out property <string> open-case: is-id ? "Buka Kasus" : "Open Case";
    out property <string> import-evidence: is-id ? "Impor Bukti" : "Import Evidence";
    out property <string> browse-evidence: is-id ? "Jelajahi Bukti" : "Browse Evidence";
    out property <string> theme-dark: is-id ? "Gelap" : "Dark";
    out property <string> theme-light: is-id ? "Terang" : "Light";
    out property <string> lang-en: "EN";
    out property <string> lang-id: "ID";
    out property <string> prefs: is-id ? "Preferensi" : "Preferences";
    out property <string> case-overview: is-id ? "Identitas Kasus" : "Case Identity";
    out property <string> next-action: is-id ? "Langkah Berikutnya" : "Next Action";
    out property <string> next-open: is-id ? "Buka atau buat kasus untuk memulai." : "Open or create a case to begin.";
    out property <string> next-import: is-id ? "Impor bukti agar pemeriksaan bisa dimulai." : "Import evidence so examination can begin.";
    out property <string> next-examine: is-id ? "Bukti siap. Mulai dari jelajah berkas, pencarian, atau linimasa." : "Evidence is ready. Start from files, search, or timeline.";
    out property <string> next-report: is-id ? "Ada markah. Susun klaim ke dalam laporan." : "Bookmarks exist. Fold claims into the report.";
    out property <string> activity: is-id ? "Aktivitas Terkini" : "Recent Activity";
    out property <string> activity-empty: is-id ? "Belum ada aktivitas pada kasus ini." : "No activity recorded for this case yet.";
    out property <string> examine-now: is-id ? "Periksa Sekarang" : "Examine Now";
    out property <string> evidence-overview: is-id ? "Cakupan" : "Coverage";
    out property <string> places-to-start: is-id ? "Pintasan Periksa" : "Examine Shortcuts";
    out property <string> status-evidence: is-id ? "Bukti" : "Evidence";
    out property <string> status-coverage: is-id ? "Cakupan" : "Coverage";
    out property <string> status-bookmarks: is-id ? "Markah" : "Bookmarks";
    out property <string> shortcuts-hint: is-id ? "/ cari · b markah · i inspector · Esc batal" : "/ search · b bookmark · i inspector · Esc clear";
    out property <string> header-search-hint: is-id ? "Cari artefak  (/)" : "Search artifacts  (/)";
    out property <string> empty-no-case: is-id ? "Belum ada kasus" : "No case open";
    out property <string> welcome-prefix: is-id ? "Selamat datang di " : "Welcome to ";
    out property <string> welcome-subtitle: is-id ? "Awal perjalanan forensik Anda." : "The beginning of your forensic journey.";
    out property <string> stat-evidence-items: is-id ? "Item Bukti" : "Evidence Items";
    out property <string> stat-data-coverage: is-id ? "Cakupan Data" : "Data Coverage";
    out property <string> stat-bookmarked: is-id ? "Artefak Ditandai" : "Bookmarked Artifacts";
    out property <string> workflow-guide: is-id ? "Panduan Alur Kerja" : "Workflow Guide";
    out property <string> workflow-step-create: is-id ? "1. Buat Kasus Baru" : "1. Create New Case";
    out property <string> workflow-step-create-body: is-id ? "Inisialisasi kasus untuk menambahkan bukti." : "Initialize a case to add evidence.";
    out property <string> workflow-step-import: is-id ? "2. Impor Data" : "2. Import Data";
    out property <string> workflow-step-import-body: is-id ? "Masukkan berkas bukti ke dalam kasus." : "Bring evidence files into your case.";
    out property <string> workflow-step-analyze: is-id ? "3. Analisis Artefak" : "3. Analyze Artifacts";
    out property <string> workflow-step-analyze-body: is-id ? "Periksa data yang diimpor untuk petunjuk." : "Examine imported data for clues.";
    out property <string> more-actions: is-id ? "Aksi Lainnya ⌄" : "More Actions ⌄";
    out property <string> shortcut-needs-case: is-id ? "Aktif setelah membuka kasus." : "Enabled after opening a case.";
    out property <string> empty-no-evidence: is-id ? "Belum ada bukti" : "No evidence imported";
    out property <string> empty-no-selection: is-id ? "Pilih berkas di daftar" : "Select a file from the list";
    out property <string> empty-no-search: is-id ? "Tidak ada hasil" : "No search results";
    out property <string> empty-no-timeline: is-id ? "Tidak ada peristiwa" : "No timeline events";
    out property <string> empty-no-bookmarks: is-id ? "Belum ada markah" : "No bookmarks yet";
    out property <string> empty-no-findings: is-id ? "Belum ada temuan" : "No findings yet";
    out property <string> empty-hint-case: is-id ? "Buka kasus untuk memulai pemeriksaan." : "Open a case to begin examination.";
    out property <string> empty-hint-evidence: is-id ? "Impor bukti untuk mengisi daftar berkas." : "Import evidence to populate the file list.";
    out property <string> empty-hint-search: is-id ? "Jalankan pencarian untuk melihat artefak." : "Run a search to see artifact hits.";
    out property <string> empty-hint-timeline: is-id ? "Peristiwa linimasa akan muncul di sini." : "Timeline events will appear here.";
    out property <string> empty-hint-bookmarks: is-id ? "Tandai temuan penting saat meninjau bukti." : "Bookmark important findings while reviewing evidence.";
    out property <string> empty-hint-report: is-id ? "Temuan terikat dari markah akan muncul di sini." : "Bound findings from bookmarks will appear here.";
    out property <string> search-placeholder: is-id ? "Cari artefak…" : "Search artifacts…";
    out property <string> search-action: is-id ? "Cari" : "Search";
    out property <string> hits: is-id ? "hasil" : "hits";
    out property <string> export-soon: is-id ? "Ekspor — segera hadir" : "Export — coming soon";
    out property <string> report-state: is-id ? "Status laporan" : "Report state";
    out property <string> col-name: is-id ? "Nama" : "Name";
    out property <string> col-size: is-id ? "Ukuran" : "Size";
    out property <string> col-path: is-id ? "Jalur" : "Path";
    out property <string> col-kind: is-id ? "Jenis" : "Kind";
    out property <string> col-summary: is-id ? "Ringkasan" : "Summary";
    out property <string> col-provenance: is-id ? "Provenans" : "Provenance";
    out property <string> col-claim: is-id ? "Klaim" : "Claim";
    out property <string> col-event: is-id ? "Peristiwa" : "Event";
    out property <string> deleted: is-id ? "terhapus" : "deleted";
    out property <string> about-prefix: is-id ? "Tentang & pengungkapan" : "About & disclosure";
    out property <string> filter-placeholder: is-id ? "Filter nama / jalur…" : "Filter name / path…";
    out property <string> filter-stub: is-id ? "Filter rentang (segera)" : "Range filter (soon)";
    out property <string> go-evidence: is-id ? "Jelajahi bukti" : "Browse evidence";
    out property <string> go-search: is-id ? "Cari artefak" : "Search artifacts";
    out property <string> go-timeline: is-id ? "Buka linimasa" : "Open timeline";
    out property <string> go-bookmarks: is-id ? "Lihat markah" : "View bookmarks";
    out property <string> go-report: is-id ? "Siapkan laporan" : "Prepare report";
    out property <string> detail-title: is-id ? "Detail berkas" : "File detail";
    out property <string> files-title: is-id ? "Berkas bukti" : "Evidence files";
    out property <string> results-title: is-id ? "Hasil" : "Results";
    out property <string> add-bookmark: is-id ? "Tambah Markah" : "Add Bookmark";
    out property <string> find-related: is-id ? "Cari Terkait" : "Find Related";
    out property <string> open-in-evidence: is-id ? "Buka di Bukti" : "Open in Evidence";
    out property <string> jump-evidence: is-id ? "Lompat ke Bukti" : "Jump to Evidence";
    out property <string> queue-for-report: is-id ? "Antrean untuk laporan" : "Queue for report";
    out property <string> work-product-hint: is-id ? "Markah di sini menjadi bahan klaim laporan." : "Bookmarks here become report claim material.";
    out property <string> command-palette: is-id ? "Palet perintah" : "Command palette";
    out property <string> command-placeholder: is-id ? "Ketik perintah…" : "Type a command…";
    out property <string> command-hint: is-id ? "Enter jalankan · Esc tutup" : "Enter run · Esc dismiss";
    out property <string> exceptions-title: is-id ? "Pengecualian / gagal" : "Exceptions / failures";
    out property <string> exceptions-empty: is-id ? "Tidak ada kegagalan cakupan." : "No coverage failures.";
    out property <string> log-title: is-id ? "Log" : "Log";
    out property <string> log-empty: is-id ? "Belum ada entri log." : "No log entries yet.";
    out property <string> toggle-inspector: is-id ? "Inspector" : "Inspector";
    out property <string> toggle-log: "Log";
    out property <string> toggle-nav: is-id ? "Nav" : "Nav";
    out property <string> designation: is-id ? "Desainasi" : "Designation";
    out property <string> integrity: is-id ? "Integritas" : "Integrity";
    out property <string> search-coverage: is-id ? "Cakupan pencarian" : "Search coverage";
    out property <string> runs-title: is-id ? "Perbandingan run" : "Run compare";
    out property <string> runs-empty: is-id ? "Belum ada manifes run." : "No run manifests yet.";
    out property <string> export-action: is-id ? "Ekspor laporan" : "Export report";
    out property <string> transfer-export: is-id ? "Ekspor paket transfer" : "Export transfer package";
    out property <string> transfer-verify: is-id ? "Verifikasi paket (tamper)" : "Verify package (tamper)";
    out property <string> import-timeline: is-id ? "Impor CSV linimasa (Plaso/Hayabusa)" : "Import timeline CSV (Plaso/Hayabusa)";
    out property <string> cheatsheet-title: is-id ? "Pintasan keyboard" : "Keyboard shortcuts";
    out property <string> cheatsheet-body: is-id ? "/ palet · 1–6 layar · b markah · Esc · ? bantuan · nav/log/inspector via palet" : "/ palette · 1–6 screens · b bookmark · Esc · ? help · nav/log/inspector via palette";
    out property <string> empty-not-in-build: is-id ? "Belum di build ini" : "Not in this build";
    out property <string> tagged: is-id ? "Ditandai" : "Tagged";
    out property <string> hex-title: "Hex";
    out property <string> yara-hits: is-id ? "Hasil YARA" : "YARA hits";
    out property <string> hashset-hits: is-id ? "Hasil hash-set" : "Hash-set hits";
    out property <string> report-blockers-title: is-id ? "Penghambat finalize" : "Finalize blockers";
    out property <string> sod-hint: is-id ? "SoD: pemeriksa ≠ penyetuju" : "SoD: examiner ≠ approver";
    out property <string> workflow-title: is-id ? "Alur kasus" : "Case workflow";
    out property <string> workflow-open: is-id ? "Buka" : "Open";
    out property <string> workflow-import: is-id ? "Impor" : "Import";
    out property <string> workflow-examine: is-id ? "Periksa" : "Examine";
    out property <string> workflow-bookmark: is-id ? "Markah" : "Bookmark";
    out property <string> workflow-report: is-id ? "Laporan" : "Report";
    out property <string> quick-verify: is-id ? "Verifikasi Cepat" : "Quick Verify";
    out property <string> load-demo: is-id ? "Muat kasus demo" : "Load demo case";
    out property <string> run-carving: is-id ? "Jalankan carving" : "Run carving";
    out property <string> carve-hits: is-id ? "Hasil carving" : "Carve hits";
    out property <string> quick-verify-title: is-id ? "Verifikasi cepat" : "Quick verify";
    out property <string> ephemeral-banner: is-id ? "Ephemeral · bukan kasus custody" : "Ephemeral · not a custody case";
    out property <string> brand-subtitle: is-id ? "MEJA PEMERIKSAAN" : "EXAMINATION WORKBENCH";
}
