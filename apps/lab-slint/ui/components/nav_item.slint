import { Theme } from "../theme.slint";

export component NavItem inherits Rectangle {
    in-out property <string> label;
    in-out property <string> glyph: "•";
    in-out property <bool> active: false;
    callback clicked();

    height: 34px;
    background: active ? Theme.surface-raised : transparent;
    border-radius: 5px;

    HorizontalLayout {
        padding-left: 6px;
        padding-right: 10px;
        spacing: 10px;

        Rectangle {
            width: 3px;
            height: 18px;
            y: 8px;
            background: root.active ? Theme.nav-active-bar : transparent;
            border-radius: 1px;
        }

        Text {
            text: root.glyph;
            color: root.active ? Theme.accent : Theme.text-secondary;
            vertical-alignment: center;
            font-size: 12px;
            width: 16px;
            horizontal-alignment: center;
        }

        Text {
            text: root.label;
            color: root.active ? Theme.text-primary : Theme.text-secondary;
            font-size: 13px;
            font-weight: root.active ? 600 : 500;
            vertical-alignment: center;
            horizontal-stretch: 1;
            overflow: elide;
        }
    }

    ta := TouchArea {
        clicked => {
            root.clicked();
        }
    }

    states [
        hover when ta.has-hover && !root.active: {
            background: Theme.row-hover;
        }
    ]
}
